<Window x:Class="TestFileWatcher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestFileWatcher" 
        xmlns:viewmodels="clr-namespace:TestFileWatcher.ViewModels"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:models="clr-namespace:TestFileWatcher.Models"
        xmlns:converters="clr-namespace:TestFileWatcher.Converters" xmlns:controls="clr-namespace:TestFileWatcher.Controls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>

        <converters:FileSizeToStringConverter x:Key="FileSizeToStringConverter"/>
        <converters:IconToImageConverter x:Key="IconToImageConverter"/>

        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="25"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Padding" Value="15,0"/>
        </Style>
        
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBlock Text="Folder:"/>
            <TextBox Text="{Binding FolderPath}" Margin="5,0" Width="300" IsReadOnly="True"/>
            <Button Command="{Binding SelectFolderCommand}" Content="..."/>
        </StackPanel>
    
        <TextBlock Text="Files:" Grid.Row="1" Margin="0,5"/>

        <ListView ItemsSource="{Binding FileList}" Grid.Row="2" SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}" VirtualizingPanel.IsVirtualizing="False">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction  Command="{Binding ListSelectedCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.View>
                <GridView VirtualizingPanel.VirtualizationMode="Recycling">
                    <GridViewColumn Header="Image" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate DataType="models:DataModel">
                                <!--<controls:IconViewer FilePath="{Binding Path=Path}"/>-->
                                <Image>
                                    <Image.Source>
                                        <MultiBinding Converter="{StaticResource IconToImageConverter}">
                                            <Binding Path="Path"/>
                                            <Binding Path="IsDir"/>
                                        </MultiBinding>
                                    </Image.Source>
                                </Image>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Path=Name}"/>
                    <GridViewColumn Header="Path" Width="400" DisplayMemberBinding="{Binding Path=Path}"/>
                    <GridViewColumn Header="Size" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate DataType="models:DataModel">
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding Converter="{StaticResource FileSizeToStringConverter}">
                                            <Binding Path="Size"/>
                                            <Binding Path="IsDir"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Date" Width="150" DisplayMemberBinding="{Binding Path=Date}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Content="Restart With Admin" Command="{Binding RunAsAdminCommand}" Grid.Row="3" HorizontalAlignment="Left" Margin="10"/>
   
    </Grid>
</Window>
